// Where the PMTiles archives are served from.
//   local dev : 'data/'  → the gitignored viewer/data symlink into Drive
//   published : 'https://<public-r2-base>/'  (needs HTTP Range + CORS)
window.BLUESPOT_DATA_BASE = 'data/';
